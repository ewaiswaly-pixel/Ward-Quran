name: quran_prayer_app
description: "A Flutter Islamic Application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # المكتبات الرسمية لتشغيل التواريخ، المواقيت، والمصحف كاملاً
  intl: ^0.19.0
  adhan: ^2.1.1
  quran: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
